<nav class="navbar navbar-expand-lg bg-light navbar-light position-fixed top-0 start-0 end-0 po">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Antoine bernabeu</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link " aria-current="page" href="{{path('app_realisation')}}">Realisation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Actualites</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Portfolio
                        </a>
                        <ul class="dropdown-menu">
                            {% for categorie in categories() %}
                                <li><a class="dropdown-item" href="{{path('app_portfolio_categorie', {'slug':categorie.slug})}}"> {{categorie.nom}} </a></li>
                            {% endfor %}
                            
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_apropos')}}">A propos</a>
                    </li>
                    
                </ul>
                <a href="" class="btn btn-primary ms-auto">Me Contacter</a>
            </div>
        </div>
</nav>

<style>
    .po{
        z-index: 1;
    }
</style>